

import 'package:flutter/material.dart';

class TemporaryPageForTesting extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Hello World'),
        ),
      ),
    );
  }
}
